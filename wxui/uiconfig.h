#ifndef UI_CONFIG_H
#define UI_CONFIG_H

#include <string>

namespace UiConfig
{
    const int WINDOW_WIDTH  = 800;
    const int WINDOW_HEIGHT = 600;

    const int MIN_WINDOW_WIDTH  = 400;
    const int MIN_WINDOW_HEIGHT = 290;

    const char* const TAB_SETTINGS = "Settings";
    const char* const TAB_SEND = "Send";
    const char* const TAB_HELP = "Help";
}

#endif // UI_CONFIG_H
