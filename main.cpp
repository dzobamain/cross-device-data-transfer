#include <iostream>
#include <string>

#include "file/file_utils.h"
#include "file/zip.h"

int main()
{
    std::cout << "Cross device data transfer, started\n";

    /*
    std::string source_path = "";
    std::string file = GetFileName(source_path);
    std::string result = std::format("send/{}.zip", file);
    CompressToZip(source_path, result);
    */
}